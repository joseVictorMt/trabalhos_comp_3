#include <iostream>
#include <vector>

using namespace std;

#include "lambda.cc"

int main( void ) {
    // // Teste 1 ------------------------------------------------
    // vector<int> v1 = { 1, 2, 3, 4, 1, 1, 0, 8 };
    // v1 | (x % 2 == 0) | cout << x*x + 1 << ' ';
    // // Teste 2 ------------------------------------------------
    // int v1[] = { 1, 2, 3, 4, 1, 1, 0, 8 };
    // v1 | (x % 2 == 0) | cout << x*x + 1 << ' ';
    // Teste 3 ------------------------------------------------
    // int v1[] = { 1, 2, 3, 4, 1, 1, 0, 8 };
    // v1 | (x % 2 == 1) | x*x | cout << x << ' ';
    return 0;
}